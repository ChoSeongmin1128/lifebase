model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Before starting, read AGENTS.md and .claude/rules/workflow.md.
Load relevant domain rules from .claude/rules/*.md for the current scope.
Prioritize correctness, security, behavior regressions, and missing tests.
Review web-first integration flow and platform parity gates.
When uncertain whether a change should trigger multi-agent/worktree policy, ask the user directly.
"""
